//test

#include "main.c"       // legal? lol

void handleCollisions(bullet *bulletArray, int ****mapLayout, 
ship *shipA, ship *shipB, int currentMap) {

    // Loop through all bullets, check if they have collided with obstacle,
    // exterior bound, or spaceship.

    // If they have, delete them from the array.

    // Update spaceship health, OBSTACLE HEALTH MAYBE?

}
